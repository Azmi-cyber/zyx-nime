# ============================================
# LOKAL DEVELOPMENT (gunakan SQLite)
# ============================================
# Tidak perlu setup database - sudah otomatis

# ============================================
# VERCEL DEPLOYMENT (gunakan PostgreSQL)
# ============================================

# Database PostgreSQL (dari Vercel Postgres atau provider lain)
DATABASE_URL="postgresql://username:password@host:5432/zyxnime?schema=public"

# Vercel Blob (untuk upload thumbnail & video)
# Dapatkan token dari: Vercel Dashboard > Storage > Blob > Create Blob Token
BLOB_READ_WRITE_TOKEN="vercel_blob_token_anda"

# NextAuth (required untuk production)
NEXTAUTH_SECRET="generate_random_string_min_32_chars"
NEXTAUTH_URL="https://your-project.vercel.app"

# ============================================
# CARA DEPLOY KE VERCEL:
# ============================================
# 1. Push kode ke GitHub
# 2. Import project di Vercel
# 3. Setup Environment Variables di Vercel:
#    - DATABASE_URL (isi dengan PostgreSQL credentials)
#    - BLOB_READ_WRITE_TOKEN (dari Vercel Blob)
#    - NEXTAUTH_SECRET (bisa generate di: https://generate-random.vercel.app/)
#    - NEXTAUTH_URL (URL project Vercel kamu)
# 4. Deploy!
