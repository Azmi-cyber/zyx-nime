# ============================================
# LOKAL DEVELOPMENT
# ============================================
# Untuk lokal, buat file .env.local dengan:
DATABASE_URL="file:./prisma/dev.db"
BLOB_READ_WRITE_TOKEN=""
NEXTAUTH_SECRET="development-secret-key"
NEXTAUTH_URL="http://localhost:3000"

# ============================================
# VERCEL DEPLOYMENT
# ============================================

# Database PostgreSQL
# Buat di: Vercel Dashboard > Storage > Postgres > Create Database
# Copy connection string ke sini
DATABASE_URL="postgresql://..."

# Vercel Blob (untuk upload thumbnail & video)
# Buat di: Vercel Dashboard > Storage > Blob > Create Blob Token
BLOB_READ_WRITE_TOKEN="vercel_blob_token_anda"

# NextAuth
NEXTAUTH_SECRET="generate_random_string_min_32_chars"
NEXTAUTH_URL="https://your-project.vercel.app"

# ============================================
# CARA DEPLOY KE VERCEL:
# ============================================
# 1. Push kode ke GitHub
# 2. Import project di Vercel.com
# 3. Di Vercel Dashboard > Settings > Environment Variables:
#    - DATABASE_URL = (PostgreSQL connection string)
#    - BLOB_READ_WRITE_TOKEN = (dari Blob storage)
#    - NEXTAUTH_SECRET = (bisa generate di: https://generate-random.vercel.app/)
#    - NEXTAUTH_URL = (URL project Vercel, contoh: https://zyx-nime.vercel.app)
# 4. Deploy!
# 5. Setelah deploy, Setup PostgreSQL:
#    - Di Vercel Storage > Postgres > your-db > "Next.js" tab
#    - Copy command: npx prisma db push
#    - Run di terminal: npx prisma db push --schema=prisma/schema.prisma
